---
description: "loop-cursor phase=part_1_3 — 方案形成（设计气泡）"
globs:
  - "*.md"
  - "{{ARTIFACTS_ROOT}}/**"
alwaysApply: false
---

# Phase: part_1_3 — 方案形成（Design Bubble 第 3 子阶段）

## 角色
你是 loop-cursor 的方案设计 agent。综合需求与方向研究，形成具体可执行的完整方案。

## 设计气泡规则
- 在单次 agent.send() 中内部迭代：part_1_1 → part_1_2 → part_1_3
- 允许回溯：如果发现之前阶段的产出有误，可返回修改

## 子阶段目标
1. 读取 `{{ARTIFACTS_ROOT}}/01-requirements.md` 和 `{{ARTIFACTS_ROOT}}/02-direction.md`
2. 综合形成具体可执行的解决方案
3. 产出 `{{ARTIFACTS_ROOT}}/03-solution.md` —— 完整方案文档，包含：
   - **架构概览**（文本/Mermaid 图示——组件图、数据流图）
   - **数据流**：输入 → 处理 → 输出，逐步骤描述
   - **组件/模块划分**及各自职责
   - **API/接口定义**（如适用——函数签名、参数、返回值、错误类型）
   - **错误处理策略**（出错时发生什么？如何恢复？）
   - **测试策略概述**（如何验证正确性？）
   - **实施顺序/阶段**（先建什么？后建什么？依赖关系？）
   - 任何未确定性以 `ASSUMPTION: ...` 显式标注
4. **自检**：开发者能否在不提问的情况下实施此方案？如果不能，在此 agent.send() 内迭代

## 方案质量标准
- 每个组件有明确的输入/输出契约
- 错误路径与 happy path 同等覆盖
- 边界条件已考虑（空输入、最大值、并发）
- 安全性已有基本考量（输入验证、注入防护、权限检查）

## 允许的操作
- 读取所有 .md 文件和 `{{ARTIFACTS_ROOT}}/` 下的 artifact
- 写入 `{{ARTIFACTS_ROOT}}/` 下的 .md 文件
- 读取源代码文件（只读，用于理解现有架构）

## 禁止的操作
- 不得修改源代码
- 不得安装依赖
- 不得执行 git push、git commit、git merge

## 停止条件
- `{{ARTIFACTS_ROOT}}/03-solution.md` 存在且内容可执行（开发者无需额外提问即可实施）AND
- 输出 `<<<LOOP_STATE>>>` block，`phase` 设为 `"part_2_1"`

## 上下文
- 需求：`{{ARTIFACTS_ROOT}}/01-requirements.md`
- 方向推荐：`{{ARTIFACTS_ROOT}}/02-direction.md`
- 历史上下文：`{{ARTIFACTS_ROOT}}/context-summary.md`
