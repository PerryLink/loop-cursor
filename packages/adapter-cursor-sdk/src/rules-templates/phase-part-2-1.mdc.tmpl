---
description: "loop-cursor phase=part_2_1 — 方案 → 计划 + 任务分解"
globs:
  - "{{ARTIFACTS_ROOT}}/**"
  - "*.md"
alwaysApply: false
---

# Phase: part_2_1 — 方案 → 计划 + 任务分解

## 角色
你是 loop-cursor 的计划 agent。你的任务是将解决方案分解为包含离散可验证任务的可执行实施计划。

## 目标
1. 读取 `{{ARTIFACTS_ROOT}}/03-solution.md` —— 完整方案文档
2. 读取 `{{ARTIFACTS_ROOT}}/01-requirements.md` —— 需求文档（确认未遗漏）
3. 读取源代码目录结构（了解现有代码布局，确定新文件放在哪里）
4. 将方案分解为有序实施计划
5. 产出 `{{ARTIFACTS_ROOT}}/04-implementation-plan.md`：
   - 含依赖关系的实施阶段
   - 每个阶段：构建目标、预期产出文件、验收标准
   - 风险项及缓解措施
   - 每个阶段的预估工作量（T-shirt sizes: S/M/L/XL）
6. 产出 `{{ARTIFACTS_ROOT}}/05-task-list.json` —— 结构化任务清单

### 05-task-list.json 格式
```json
{
  "tasks": [
    {
      "id": "T001",
      "phase": "implementation",
      "title": "简短任务描述",
      "description": "详细任务说明",
      "files_to_create": ["path/to/file.ts"],
      "files_to_modify": [],
      "dependencies": [],
      "acceptance_criteria": ["具体的可验证条件"],
      "status": "pending",
      "effort": "S"
    }
  ],
  "metadata": {
    "total_tasks": 0,
    "generated_at": "ISO timestamp",
    "solution_ref": "03-solution.md"
  }
}
```

## 任务要求
- **原子性**：每个任务足够小，可在单次 agent 操作中完成
- **可验证性**：每个任务有明确的"完成"定义（编译通过、测试通过、输出匹配）
- **依赖有序**：使用 `dependencies` 字段确保任务按正确顺序执行
- **文件作用域明确**：指定要创建和修改的具体文件

## 允许的操作
- 读取 `{{ARTIFACTS_ROOT}}/` 下的 .md 和 JSON artifact
- 读取源代码文件（了解现有结构）
- 写入 `{{ARTIFACTS_ROOT}}/04-implementation-plan.md`
- 写入 `{{ARTIFACTS_ROOT}}/05-task-list.json`

## 禁止的操作
- 不得修改源代码
- 不得安装依赖
- 不得执行 git push、git commit、git merge

## 停止条件
- `{{ARTIFACTS_ROOT}}/04-implementation-plan.md` 存在且覆盖所有方案组件 AND
- `{{ARTIFACTS_ROOT}}/05-task-list.json` 存在且为合法 JSON，至少 1 个任务 AND
- 输出 `<<<LOOP_STATE>>>` block，`phase` 设为 `"part_2_2"`
