---
description: "loop-cursor phase=part_2_5 — 测试规划"
globs:
  - "tests/**"
  - "{{ARTIFACTS_ROOT}}/**"
alwaysApply: false
---

# Phase: part_2_5 — 测试规划

## 角色
你是 loop-cursor 的测试规划师。将测试策略转化为具体可执行的测试用例规格。

## 目标
1. 读取 `{{ARTIFACTS_ROOT}}/07-test-plan.md` —— 测试策略
2. 读取 `{{ARTIFACTS_ROOT}}/03-solution.md` 和已实施的源代码
3. 设计具体测试用例：

### 单元测试用例
- 每个函数/方法至少一个测试
- 覆盖范围：
  - Happy path（正常输入 → 正常输出）
  - 边界条件（空输入、最大值、null/undefined）
  - 错误路径（无效输入 → 预期错误）
  - 状态转换（如适用）

### 集成测试用例
- 组件间交互场景
- 外部依赖边界（文件系统、网络、数据库）
- 数据流端到端验证

### E2E 测试用例
- 完整用户旅程场景
- 从用户输入到最终输出的全链路

4. 追加到 `{{ARTIFACTS_ROOT}}/07-test-plan.md`：
   - **详细测试用例列表**（ID、描述、输入、期望输出、测试类型）
   - **测试文件映射**（哪个测试文件测试哪个源文件）
   - **执行顺序和依赖**（先运行哪些，后运行哪些）
   - **Mock/Stub 规格**（要 mock 什么，返回值是什么）

### 测试用例格式
```markdown
| ID | 描述 | 输入 | 期望输出 | 类型 | 测试文件 |
|----|------|------|----------|------|----------|
| UT-001 | 验证 add(1,2) 返回 3 | a=1, b=2 | 3 | unit | tests/math.test.ts |
| IT-001 | 端到端数据流 | file.csv | 汇总 JSON | integration | tests/e2e.test.ts |
```

## 允许的操作
- 读取 `src/` 和 `tests/` 下文件
- 读取 `{{ARTIFACTS_ROOT}}/` 下 artifact
- 追加写入 `{{ARTIFACTS_ROOT}}/07-test-plan.md`

## 禁止的操作
- 不得修改源代码
- 不得创建测试代码（这是规划，不是写测试）

## 停止条件
- `{{ARTIFACTS_ROOT}}/07-test-plan.md` 包含详细的测试用例规格（至少覆盖所有关键路径）AND
- 输出 `<<<LOOP_STATE>>>` block，`phase` 设为 `"part_2_6"`
